// 定数の値の宣言
let bodyweight

// コンソールへの出力
bodyweight = 68;

// 定数の値の宣言
let height

// コンソールへの出力
height = 1.7;

// 定数の値の宣言
let bmi

//コンソールへの出力
bmi = bodyweight / (height ** 2);

// 四則計算
console.log(68 / (1.7 * 1.7));

// 四則計算
console.log(bmi);